<UserControl
    x:Class="MangaApp.MangaIcon"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MangaApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="100">
    
   <Grid>
								  <Grid Height="100" Width="100" HorizontalAlignment="Stretch" Margin="2.5,2.5" Background="#99000000">
									<Image HorizontalAlignment="Left" Height="100" VerticalAlignment="Top" Width="100" Source="{Binding Image}" Stretch="UniformToFill" Opacity="0.4"/>
									<TextBlock VerticalAlignment="Bottom" Margin="5,0,5,5" FontSize="14.5" TextWrapping="Wrap" Text="{Binding Name}" Foreground="White"/>
								</Grid>
								</Grid>
</UserControl>
